## Summary

<!-- What does this PR do? Link the related issue. -->
<!-- If closing multiple issues, use a separate `Closes #N` for each — comma-separated lists only close the first. -->

Closes #

## Changes

-

## Checklist

- [ ] `make test` passes (with `-race`)
- [ ] `make lint` passes
- [ ] Tests added for new behaviour
- [ ] `CHANGELOG.md` updated under the next version's heading (e.g. `## [1.7.1]`)
